import type { Metadata, Viewport } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import { Providers } from '@/components/Providers'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'EUROBRIDGE - European Visa & Immigration Consulting',
  description: 'Professional visa and immigration consulting services for seamless relocation to Europe. Expert guidance for work visas, residence permits, and family reunification.',
  keywords: 'visa consulting, immigration, European relocation, work visa, residence permit',
  authors: [{ name: 'EUROBRIDGE' }],
  openGraph: {
    title: 'EUROBRIDGE - European Visa & Immigration Consulting',
    description: 'Your professional partner in European immigration',
    type: 'website',
  },
}

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 5,
  userScalable: true,
  themeColor: '#1a2d4d',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className="font-sans antialiased">
        <Providers>
          {children}
        </Providers>
        <Analytics />
      </body>
    </html>
  )
}
